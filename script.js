console.log("Page loaded...");

var screenWidth = window.screen.width;
var screenHeight = window.screen.height;

function getScreenSize(){
    console.log(screenWidth + "x" + screenHeight);

}

// run inside browser envirenment 